" 设置显示行号。
set nu

" 使用空格代替制表符。
set expandtab

" 设定 tab 长度为 4。
set tabstop=4

" 插入状态可以使用退格键。
set backspace=2

" 设置蓝色注释看不清。
hi Comment ctermfg=4

" 自动语法高亮。
syntax on

" 打开状态栏标尺。
set ruler

" 搜索时高亮显示被找到的文本。
set hlsearch
