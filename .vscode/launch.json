{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Task",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/task/main.go",
            "args": [
                "--config",
                "${workspaceFolder}/configs/config.yaml"
            ]
        }
    ]
}