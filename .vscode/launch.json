{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Kratos Layout",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/kratos-layout/main.go",
            "args": [
                "--config",
                "${workspaceFolder}/configs/config.yaml"
            ]
        }
    ]
}